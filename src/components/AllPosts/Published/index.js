import React from "react";
import PublishedTable from "./PublishedTable";

const index = () => {
  return (
    <div className="py-3">
      <PublishedTable />
    </div>
  );
};

export default index;
